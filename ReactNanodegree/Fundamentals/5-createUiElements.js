/**
 * React uses JavaScript objects to create React elements. We'll use these React elements to describe what we want the page to look like, 
 * and React will be in charge of generating the DOM nodes to achieve the result.
 */

React.createElement( / *type* /, / *props* /, / *content* / );

/**
 * Rendering Elements onto the DOM
 * By passing this DOM node into getElementById(), React will end up controlling the entirety of its contents. 
 */

/**
 * list of elements
 */

/**
 * unique key properties
 */
